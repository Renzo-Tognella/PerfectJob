spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/perfectjob
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8081

logging:
  level:
    org.springframework.security: WARN
